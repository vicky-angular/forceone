import React from "react";
import {Box} from "grommet";
const HeaderCard = () => {
    return (
          <Box flex={false} tag="header" background="brand" pad="small">
            Force One
          </Box>
    );
};

export {HeaderCard};